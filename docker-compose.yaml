version: "3.8"

services:
  servidor-java-final:
    build: .
    container_name: servidor-java-final
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./uploads/images:/app/uploads/images
